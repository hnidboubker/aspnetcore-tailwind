name: Build MVC

on:
  push:
    branches:
      - main
    paths:
      - 'src/TailwindMvc.Web/**'
      - '.github/workflows/build-mvc.yml'

  pull_request:
    paths:
      - 'src/TailwindMvc.Web/**'
      - '.github/workflows/build-mvc.yml'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore src/TailwindMvc.Web/TailwindMvc.Web.csproj

      - name: Build
        run: dotnet build src/TailwindMvc.Web/TailwindMvc.Web.csproj --configuration Release --no-restore

     
      - name: Publish
        run: dotnet publish src/TailwindMvc.Web/TailwindMvc.Web.csproj --configuration Release --no-build --output ./publish/mvc
